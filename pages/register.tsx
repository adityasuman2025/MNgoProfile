import { useEffect } from "react";

export default function Register() {
    useEffect(() => {
        // if (window) window.location.replace("https://mngo.in");
    }, []);

    return (
        <main className="mngo-bg-black">
            MNgo Register
        </main>
    );
}
